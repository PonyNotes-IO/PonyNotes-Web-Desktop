<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.xmbj.mapper.AddonMapper">
    
    <resultMap id="AddonResult" type="com.ruoyi.xmbj.domain.Addon">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="description" column="description"/>
        <result property="price" column="price"/>
        <result property="unit" column="unit"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <!-- 查询所有活跃的补充包 -->
    <select id="selectActiveAddons" resultMap="AddonResult">
        SELECT * FROM addons 
        WHERE status = 'active' 
        ORDER BY price ASC
    </select>
    
    <!-- 按类型查询补充包 -->
    <select id="selectAddonsByType" parameterType="String" resultMap="AddonResult">
        SELECT * FROM addons 
        WHERE type = #{type} 
        AND status = 'active' 
        ORDER BY price ASC
    </select>
    
</mapper>
