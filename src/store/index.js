import Vue from 'vue'
import Vuex from 'vuex'
import scale from '../utils/scale'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {},
  plugins: [scale]
})